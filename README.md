# Nonlinear Least squares parameter estimation using Levenberg-Marquardt

This project aims to estimate the parameters of data collected from a thermistor, with temperature dependent resistance.
$$R\left(T\right) = R_0 \cdot e^{\alpha T}$$

We wish to estimate the parameters $' \{R_0, \alpha\}'$ using weighted least squares.
The
